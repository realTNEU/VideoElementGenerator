{
  "name": "videoelementgenerator",
  "version": "1.0.0",
  "description": "An automatic tool to generate video ideas related to a niche, then generate a script around that idea, then converts the text into speech",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ameya Taneja",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.3.1",
    "axios": "^1.6.8",
    "dotenv": "^16.4.5",
    "node-fetch": "^2.7.0",
    "nodemon": "^3.1.0",
    "readline": "^1.3.0"
  }
}
